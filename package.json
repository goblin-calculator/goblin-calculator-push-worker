{
  "name": "goblin-calculator-push",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply goblin-calculator-push",
    "db:migrate:remote": "wrangler d1 migrations apply goblin-calculator-push --remote"
  },
  "dependencies": {
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
